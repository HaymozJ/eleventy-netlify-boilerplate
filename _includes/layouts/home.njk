---
layout: layouts/base.njk
section: home
---

{% if blurb %}
    <div class="row justify-content-center">
        {% for item in blurb %}
            <div class="col-md-4">
                <img src="{{ item.blurbImg }}"/>
                <p>{item.blurbTxt}</p>
            </div>
        {% endfor %}
    </div>
{% endif %}

{{ layoutContent | safe }}
